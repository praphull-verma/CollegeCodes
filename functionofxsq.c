#include<stdio.h>
int main(){
    /*QUESTION: Write a C program to evaluate the following expression:
    Y=x2+2x+1 for a given value of x.
    */
    int x = 5;
    int y = (x*x) + (2*x) + 1;
    printf("The value of y is: %d", y);
    return 0;



}
